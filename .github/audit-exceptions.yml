version: 1
exceptions:
  # SheetJS CE 0.18.5 has no patched npm release. The admin export path
  # writes trusted server-side data and does not parse user-uploaded files.
  # Reassess before this date; prefer replacing xlsx with a maintained fork.
  - package: xlsx
    advisory: "GHSA-4r6h-8v6p-xvw6"
    severity: high
    reason: "Admin export only; no arbitrary workbook parsing"
    mitigation: "Load only on export; restrict export permissions and data scope"
    expires_on: "2026-09-30"
    owner: "security@your-domain"
  - package: xlsx
    advisory: "GHSA-5pgg-2g8v-p4x9"
    severity: high
    reason: "Admin export only; no arbitrary workbook parsing"
    mitigation: "Load only on export; restrict export permissions and data scope"
    expires_on: "2026-09-30"
    owner: "security@your-domain"
