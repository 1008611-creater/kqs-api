## 改动说明

<!-- 一句话说清这个 PR 做了什么、为什么做。 -->

## 变更分级

<!-- 对照 AGENTS.md 第 4 节。分级决定了需要哪些验证。 -->

- [ ] **L0** 文档 / 注释 / 文案
- [ ] **L1** 前端 UI（.vue / .ts）
- [ ] **L2** 后端业务（handler / service / repository，不动 schema）
- [ ] **L3** 触及数据或部署（迁移 / schema / Dockerfile / CI / deploy）

> **L3 必须填下面的「回滚方案」，并等待明确批准后再合入。**

## 验证结果

<!-- 贴 scripts/verify.sh 的汇总表。有「未验证」项必须显式说明原因。 -->

```
（粘贴 verify.sh 输出的汇总部分）
```

### 明确未验证的部分

<!--
诚实填写。没验证 ≠ 通过。
例如：「本机 Go 工具链损坏，后端编译与单测均未在本机验证，依赖 CI backend-ci.yml。」
若全部已验证，写「无」。
-->

## 硬约束自检

<!-- 对照 docs/CONSTRAINTS.md。勾选表示已确认，不适用也请勾选并说明。 -->

- [ ] **C2** 若改了 `frontend/package.json`，`pnpm-lock.yaml` 已同提交（不适用则勾选）
- [ ] **C3** 若改了 `backend/ent/schema/*.go`，已 `go generate ./ent` 并提交生成代码（不适用则勾选）
- [ ] **C4** 若给 interface 加了方法，所有 stub 已补齐（不适用则勾选）
- [ ] **C5** **未修改任何已执行的迁移文件**；新迁移只新增文件（不适用则勾选）
- [ ] **C6** 新增迁移是幂等的（`IF NOT EXISTS` / 条件式）（不适用则勾选）
- [ ] **C7** 未提交任何真实密钥（已跑 `tools/secret_scan.py`）
- [ ] **C8** 未改写 git 历史，未 force push
- [ ] **C12** 配置文件使用 ASCII 直引号
- [ ] **C13** 文档为无 BOM 的 UTF-8

### C5 补充声明（L3 必填）

<!--
新迁移文件名：

未修改的历史迁移文件：
-->

## 回滚方案（L3 必填）

<!--
出问题怎么退回去？写具体动作。
例如：「镜像回退到 <tag>；新迁移 168_xxx.sql 只加列不删列，无需回滚 SQL。」
若改动不可回滚，明确说明并说明为什么可以接受。
-->

## 上游同步声明（若有摘取上游改动）

<!-- 对照 CONSTRAINTS.md C11。 -->

- [ ] 本 PR 未涉及上游同步
- [ ] 本 PR 从上游摘取了改动，清单如下：

| 上游提交 | 内容 | 影响的文件 | 为什么需要 |
| --- | --- | --- | --- |
|  |  |  |  |

---

<sub>提交前请确认已阅读 `AGENTS.md` 与 `docs/CONSTRAINTS.md`。</sub>
